# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Configuration (Render-compatible)
# Format: postgresql://username:password@hostname:port/database_name?sslmode=require
DATABASE_URL=postgresql://localhost:5432/binaai_dev

# Example for Render/Production:
# DATABASE_URL=postgresql://user:password@hostname:5432/database_name?sslmode=require
